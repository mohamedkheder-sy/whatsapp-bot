# 1. تحديد نظام التشغيل والبيئة (Node.js الإصدار 18)
FROM node:18

# 2. إنشاء مجلد العمل داخل السيرفر
WORKDIR /app

# 3. نسخ ملفات التعريف الخاصة بالمكتبات أولاً
# هذا يساعد في تسريع عملية البناء (Cache)
COPY package*.json ./

# 4. تثبيت المكتبات البرمجية
RUN npm install

# 5. نسخ بقية ملفات المشروع (مثل app.js) إلى السيرفر
COPY . .

# 6. فتح المنفذ الذي سيستخدمه السيرفر (نفس المنفذ في كود app.js)
EXPOSE 8000

# 7. الأمر النهائي لتشغيل البوت
CMD ["node", "app.js"]
